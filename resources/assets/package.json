{
  "name": "app",
  "private": true,
  "scripts": {
    "build:watch": "esbuild javascripts/*.* --bundle --sourcemap --format=esm --outdir=builds --watch",
    "css:watch": "npx @tailwindcss/cli -i ./stylesheets/tailwind.css -o ./builds/tailwind.css --watch"
  },
  "dependencies": {
    "@hotwired/stimulus": "^3.2.2",
    "@hotwired/turbo": "^8.0.13",
    "@tailwindcss/cli": "^4.1.10",
    "tailwindcss": "^4.1.10"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.16",
    "daisyui": "^5.0.35",
    "esbuild": "^0.25.5",
    "tailwindcss-motion": "^1.1.0"
  }
}
