# 웹 성능 최적화 가이드 V2

기존의 설정에서 놓친 부분들과, 잘못 인지했던 부분들을 잡는데 중심을 두었습니다.

## 정적 에셋 캐시 최적화:

V1에서는 main.ts에서 정적 파일들의 응답 헤더에 캐시 날짜(`max-age`, `immutable` 등)를 설정했습니다.
이후 개선된 내용은 다음과 같습니다:

### Cache `immutable` 지시어 제거

해당 지시어 사용 시 보통 assets 파일명에 해시를 포함하여 새로 빌드할 때마다 브라우저에게 변경을 알려줘야 하는데, 현재 프로젝트의 에셋 파이프라인에는 이 기능이 누락되어 있습니다.

변경된 파일 app.js, tailwind.css에 해시가 포함되면 HTML 파일에도 자동으로 적용되도록 설정해야 하는데, Vite manifest 파일 및 HTML을 생성하는 헬퍼 등이 필요합니다. 지금 당장 구현할 계획은 없습니다. (Ruby on Rails나 Angular 등의 많은 프레임워크들이 이러한 작업을 자동화해주는 것 같습니다)

결론적으로 immutable 지시어는 제거했습니다.

## 폰트로딩 최적화(FOIT):

V1에서는 폰트에 `font-display: swap` 설정을 사용했습니다.
이후 개선된 내용은 다음과 같습니다:

### TTF 파일 → WOFF2로 폰트 확장자 변경
차이가 이렇게 클 줄은 몰랐습니다. 구글에서 내려받은 파일이 TTF만 있어서 그대로 사용했는데, Google Fonts CDN으로 사용했을 때 오히려 점수가 더 좋게 나와서 놀랐습니다. 

이후 `https://gwfh.mranftl.com/fonts` 사이트에서 WOFF2 형식으로 다운로드하여 로컬에서 제공하는 방식으로 변경했더니 이 변경 하나만으로도 성능 점수가 크게 향상되었습니다.

## 렌더링 차단 리소스 최적화:

V1에서는 Critical CSS 인라인화, 리소스 프리로딩(`rel="preload"`), 비동기 CSS 로딩(`onload="this.onload=null;this.rel='stylesheet'"`) 등을 적용했습니다.
이후 개선된 내용은 다음과 같습니다:

### 1. Critical CSS 인라인 제거

폰트 파일을 개선하니 인라인으로 폰트를 가져오는 코드가 더 이상 필요하지 않게 되었습니다. 관리는 이전과 동일하게 하되 오히려 속도가 더 향상되었습니다.

### 2. 리소스 프리로딩과 비동기 로딩 폴백 제거

미미한 향상을 위해 적용했던 기법이 오히려 문제를 일으켰습니다. 이 프로젝트는 Turbo Drive를 통해 첫 방문 이후 페이지 이동이 SPA처럼 작동하는데, 프리로딩 설정을 위해 작성했던 다음 코드가 문제였습니다:

```
onload="this.onload=null;this.rel='stylesheet'"
```
이 코드가 Turbo Drive에 의해 재실행되면서 tailwind.css 파일을 페이지 이동 시마다 계속 다운로드하는 현상이 발생했습니다.

따라서 프리로딩을 제거했고, 그에 따라 비동기 로딩도 불필요해졌습니다.

## 정리

기존의 개선 방식에서 위의 내용들을 원래대로 되돌렸지만, 에셋 파일 캐시, 자바스크립트 defer 지연, 폰트 로딩 최적화, 압축 최적화 등으로 확실히 성능 점수가 향상되었습니다.

